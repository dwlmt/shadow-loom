# =====================================================================
# Shadow-Loom — development requirements
# =====================================================================
# Mirrors pyproject.toml `[project.optional-dependencies].dev`.
# Install with:
#     pip install -r requirements.txt -r requirements-dev.txt
# or equivalently:
#     pip install -e ".[dev]"
# =====================================================================

-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.24
ruff>=0.7
mypy>=1.13
